<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.spagreen.linphonesdk">

    <!-- =============================
         CORE VoIP PERMISSIONS
         ============================= -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.USE_SIP" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CAMERA"/>
    
    <!-- =============================
         FOREGROUND SERVICE PERMISSIONS (Android 14+)
         ============================= -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    
    <!-- =============================
         WAKE LOCK - Keep device awake for calls
         ============================= -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- =============================
         BLUETOOTH PERMISSIONS - Required for Bluetooth audio routing
         ============================= -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    
    <!-- =============================
         NETWORK PERMISSIONS - Required for proper network handling
         ============================= -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    
    <!-- =============================
         AUDIO PERMISSIONS - Required for audio focus and routing
         ============================= -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    
    <!-- =============================
         VIBRATE - For incoming call notification
         ============================= -->
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- =============================
         BOOT COMPLETED - For background registration
         ============================= -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- =============================
         PUSH NOTIFICATIONS (FCM)
         ============================= -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- =============================
         FULL-SCREEN INTENT - For incoming call screen over lock screen
         ============================= -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:usesCleartextTraffic="true">

        <!-- Linphone SDK's built-in CoreService COMPLETELY REMOVED.
             We handle iteration, keep-alive, and foreground notifications
             ourselves via SipForegroundService + LinPhoneHelper.
             tools:node="remove" prevents the SDK from merging its own
             declaration back in via its AAR manifest. -->
        <service
            android:name="org.linphone.core.tools.service.CoreService"
            tools:node="remove" />

        <!-- Our foreground service to keep SIP alive for incoming calls -->
        <service
            android:name="com.spagreen.linphonesdk.SipForegroundService"
            android:foregroundServiceType="phoneCall|microphone"
            android:stopWithTask="false"
            android:exported="false" />

        <!-- Native incoming call screen – shows over lock screen -->
        <activity
            android:name="com.spagreen.linphonesdk.IncomingCallActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:exported="false" />

        <!-- Native active (in-call) screen -->
        <activity
            android:name="com.spagreen.linphonesdk.ActiveCallActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:exported="false" />

    </application>

</manifest>
